<Window x:Class="AudioDeviceSwitcher.OsdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OSD" Height="70" Width="330"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        IsHitTestVisible="False"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI Variable Text, Segoe UI, sans-serif">

    <Window.RenderTransform>
        <TranslateTransform Y="0"/>
    </Window.RenderTransform>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" Duration="0:0:0.18">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <!-- Frosted Acrylic HUD Pill -->
    <Border CornerRadius="12" Background="#E6161616" BorderBrush="#25FFFFFF" BorderThickness="1">
        <Grid Margin="14,10,16,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Accent Icon Circle -->
            <Border Grid.Column="0" Width="38" Height="38" CornerRadius="19" Background="#1A60CDFF" BorderBrush="#3560CDFF" BorderThickness="1" VerticalAlignment="Center" Margin="0,0,12,0">
                <TextBlock x:Name="GlyphText" Text="&#xE995;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI Symbol" FontSize="16" Foreground="#60CDFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            
            <!-- Information -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Text="AUDIO PLAYBACK SWITCHED" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="10.5" FontWeight="SemiBold" Foreground="#888888"/>
                <TextBlock x:Name="DeviceNameText" Text="Headphones" FontFamily="Segoe UI Variable Text, Segoe UI" Foreground="#FFFFFF" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
