<Window x:Class="AudioDeviceSwitcher.OsdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OSD" Height="76" Width="330"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        IsHitTestVisible="False"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI Variable Text, Segoe UI, sans-serif">

    <Window.RenderTransform>
        <TranslateTransform Y="0"/>
    </Window.RenderTransform>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.15">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" Duration="0:0:0.18">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <!-- Acrylic HUD pill (transient surface → Acrylic per Fluent materials guidance).
         8px radius container; inner fill is a fallback for the legacy blur path. -->
    <Border CornerRadius="{DynamicResource OverlayCornerRadius}"
            Background="{DynamicResource OsdFillBrush}"
            BorderBrush="{DynamicResource StrokeStrongBrush}"
            BorderThickness="1">
        <Grid Margin="16,12,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Accent icon circle -->
            <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20"
                    Background="{DynamicResource AccentCardBrush}"
                    BorderBrush="{DynamicResource AccentStrokeBrush}" BorderThickness="1"
                    VerticalAlignment="Center" Margin="0,0,12,0">
                <TextBlock x:Name="GlyphText" Text="&#xE995;"
                           FontFamily="{DynamicResource FontFamilyIcons}"
                           FontSize="16" Foreground="{DynamicResource AccentBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Information -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <!-- Caption 12px (was 10.5 — below the 12px legibility floor) -->
                <TextBlock x:Name="TitleText" Text="Audio playback switched"
                           FontFamily="{DynamicResource FontFamilyText}"
                           FontSize="{DynamicResource FontSizeCaption}"
                           Foreground="{DynamicResource TextTertiaryBrush}"/>
                <!-- Body-strong 14px -->
                <TextBlock x:Name="DeviceNameText" Text="Headphones"
                           FontFamily="{DynamicResource FontFamilyText}"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           FontSize="{DynamicResource FontSizeBody}" FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis" Margin="0,1,0,0"/>

                <!-- Volume progress bar (visible only for volume events) -->
                <Border x:Name="VolumeBarTrack" Height="4" CornerRadius="2"
                        Background="{DynamicResource StrokeStrongBrush}"
                        Margin="0,7,0,1" Visibility="Collapsed" HorizontalAlignment="Stretch">
                    <Border x:Name="VolumeBarFill" Height="4" CornerRadius="2"
                            Background="{DynamicResource AccentBrush}"
                            HorizontalAlignment="Left" Width="0"/>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>
