<Window x:Class="AudioDeviceSwitcher.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        FontFamily="Segoe UI Variable Display, Segoe UI">

    <Window.Resources>
        <!-- Similar style to MixerWindow -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#25FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#EE1E1E1E" BorderBrush="#25FFFFFF" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,20"/>
                
                <Button x:Name="UpdateButton" Grid.Column="1" Content="Update Available!" Click="Update_Click" 
                        Background="#FF1775" Foreground="White" FontWeight="Bold" 
                        Visibility="Collapsed" Margin="0,0,0,20"/>
            </Grid>
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Global Features -->
                    <TextBlock Text="Features" Foreground="#A0A0A0" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <CheckBox x:Name="RunAtStartupCheck" Content="Run on Windows Startup" Foreground="White" Margin="0,0,0,8"/>
                    <CheckBox x:Name="EnableHotkeysCheck" Content="Enable Global Hotkeys (Ctrl+Shift+S / Ctrl+Shift+M)" Foreground="White" Margin="0,0,0,20"/>
                    
                    <!-- Devices List -->
                    <TextBlock Text="Audio Devices" Foreground="#A0A0A0" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock Text="Select which devices appear in the Mixer and Quick Switch loop." Foreground="#808080" FontSize="12" Margin="0,0,0,10" TextWrapping="Wrap"/>
                    
                    <Border Background="#15FFFFFF" CornerRadius="4" Padding="10">
                        <StackPanel x:Name="DevicesPanel"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Width="80" Click="Cancel_Click" Margin="0,0,10,0"/>
                <Button Content="Save" Width="80" Click="Save_Click" Background="#60CDFF" Foreground="Black"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
