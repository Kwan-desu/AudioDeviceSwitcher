<Window x:Class="AudioDeviceSwitcher.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="540" Width="620"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI Variable Text, Segoe UI, sans-serif">

    <Window.RenderTransform>
        <TranslateTransform Y="0"/>
    </Window.RenderTransform>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.18">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="12" To="0" Duration="0:0:0.22">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.Resources>
        <!-- Fluent Primary Button -->
        <Style x:Key="FluentPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#60CDFF"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#78D7FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4FBBEB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Fluent Secondary Button -->
        <Style x:Key="FluentSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#25FFFFFF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#10FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Box -->
                            <Border x:Name="CheckBorder" Width="18" Height="18" CornerRadius="4" Background="#18FFFFFF" BorderBrush="#35FFFFFF" BorderThickness="1" VerticalAlignment="Center">
                                <Path x:Name="CheckMark" Data="M 3,9 L 7,13 L 15,4" Stroke="#000000" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,1,0,0"/>
                            </Border>

                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="#60CDFF"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#60CDFF"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#60CDFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Style -->
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border CornerRadius="2" Background="#30FFFFFF" Width="4" HorizontalAlignment="Center"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Frosted Acrylic Glass Background Container -->
    <Border CornerRadius="8" Background="#EE181818" BorderBrush="#25FFFFFF" BorderThickness="1">
        <Grid Margin="24,20,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Settings" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="20" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                    <TextBlock Text="Audio Device Switcher Configuration" FontSize="12" Foreground="#8A8A8A" Margin="0,2,0,0"/>
                </StackPanel>
                
                <!-- Update Pill Button -->
                <Button x:Name="UpdateButton" Grid.Column="1" Click="Update_Click" 
                        Background="#1A60CDFF" BorderBrush="#5060CDFF" BorderThickness="1" Foreground="#60CDFF" FontWeight="SemiBold" FontSize="12.5"
                        Padding="12,6" Cursor="Hand" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,12,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
                                <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                                    <TextBlock Text="&#xE896; " FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI Symbol" FontSize="12" VerticalAlignment="Center"/>
                                    <TextBlock Text="Update Available!" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Close / Dismiss Button -->
                <Button Grid.Column="2" Content="&#xE8BB;" Click="Cancel_Click" 
                        FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets, Segoe UI Symbol" FontSize="12"
                        Foreground="#AAAAAA" Background="Transparent" BorderThickness="0" Width="30" Height="30" Cursor="Hand" VerticalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="btnBorder" CornerRadius="5" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="btnBorder" Property="Background" Value="#25FFFFFF"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            
            <!-- Body Settings Cards -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0,0,6,0">
                    
                    <!-- General Preferences Card Group -->
                    <TextBlock Text="General" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="13.5" FontWeight="SemiBold" Foreground="#A0A0A0" Margin="2,0,0,8"/>
                    <Border Background="#0CFFFFFF" BorderBrush="#14FFFFFF" BorderThickness="1" CornerRadius="7" Padding="16,14" Margin="0,0,0,18">
                        <StackPanel>
                            <!-- Run at Startup -->
                            <Grid Margin="0,0,0,14">
                                <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                                    <TextBlock Text="Launch on Windows startup" FontSize="13.5" FontWeight="SemiBold" Foreground="#EEEEEE"/>
                                    <TextBlock Text="Automatically start Audio Device Switcher when you log in" FontSize="12" Foreground="#858585" Margin="0,2,0,0"/>
                                </StackPanel>
                                <CheckBox x:Name="RunAtStartupCheck" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <Border Height="1" Background="#10FFFFFF" Margin="0,0,0,14"/>

                            <!-- Global Hotkeys -->
                            <Grid Margin="0,0,0,14">
                                <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                                    <TextBlock Text="Global keyboard shortcuts" FontSize="13.5" FontWeight="SemiBold" Foreground="#EEEEEE"/>
                                    <TextBlock Text="Quick Switch: Ctrl+Shift+S   •   Volume Mixer: Ctrl+Shift+M" FontSize="12" Foreground="#858585" Margin="0,2,0,0"/>
                                </StackPanel>
                                <CheckBox x:Name="EnableHotkeysCheck" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <Border Height="1" Background="#10FFFFFF" Margin="0,0,0,14"/>

                            <!-- Scroll on Tray Icon -->
                            <Grid>
                                <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                                    <TextBlock Text="Scroll on tray icon to adjust volume" FontSize="13.5" FontWeight="SemiBold" Foreground="#EEEEEE"/>
                                    <TextBlock Text="Hover mouse over taskbar tray icon and scroll wheel to change volume" FontSize="12" Foreground="#858585" Margin="0,2,0,0"/>
                                </StackPanel>
                                <CheckBox x:Name="EnableTrayScrollCheck" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Audio Devices Card Group -->
                    <TextBlock Text="Audio Devices" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="13.5" FontWeight="SemiBold" Foreground="#A0A0A0" Margin="2,0,0,4"/>
                    <TextBlock Text="Select which playback devices participate in Quick Switch cycling and appear in the Volume Mixer." FontSize="12" Foreground="#858585" Margin="2,0,0,8" TextWrapping="Wrap"/>
                    
                    <Border Background="#0CFFFFFF" BorderBrush="#14FFFFFF" BorderThickness="1" CornerRadius="7" Padding="14,10" Margin="0,0,0,10">
                        <StackPanel x:Name="DevicesPanel"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancel" Style="{StaticResource FluentSecondaryButton}" Width="90" Click="Cancel_Click" Margin="0,0,10,0"/>
                <Button Content="Save changes" Style="{StaticResource FluentPrimaryButton}" Width="120" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
